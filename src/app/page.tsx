import { Metadata } from 'next';

export const metadata: Metadata = {
	title: 'VinuChain Staking Station',
	description: 'Stake your VinuChain tokens to earn rewards.'
};

export default function HomePage() {
	return <HomePage />;
}
